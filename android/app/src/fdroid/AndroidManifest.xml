<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- GroMore required permissions -->
    <permission android:name="${applicationId}.openadsdk.permission.TT_PANGOLIN" tools:node="remove" />
    <uses-permission android:name="${applicationId}.openadsdk.permission.TT_PANGOLIN" tools:node="remove" />

    <application>
        <!-- GroMore TTFileProvider (to remove) -->
        <provider
            android:name="com.bytedance.sdk.openadsdk.TTFileProvider"
            tools:node="remove">
        </provider>
    </application>

</manifest>
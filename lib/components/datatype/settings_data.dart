import 'package:flutter/material.dart';
import '../spec_delegate.dart' show DataChangeCallback;

/*
class Settings {
  static String filename = "settings.json";

  String langCode = "zh";
  String countryCode = "";
  List<String> shownGalleryItems = [];

  Settings({langCode, this.countryCode, shownGalleryItems}){
    this.langCode=langCode??'zh';
    this.shownGalleryItems??[];
  }

  Locale getLocale() {
    print('lang=$langCode, country=$countryCode');
    return Locale(langCode,countryCode);
  }

  Settings.fromJson(Map data) {
    langCode = data["langCode"];
    countryCode = data["countryCode"];
    shownGalleryItems = data["shownGalleryItems"] ?? [];
  }

  Map<String, dynamic> toJson() => {
    "langCode": langCode,
    "countryCode": countryCode,
    "shownGalleryItems": shownGalleryItems
  };
}
*/
